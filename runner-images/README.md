# Runner Images

The purpose of this folder is to provide configuration for building and pushing runner images. These images are intended to be used in konflux tasks and github actions workflows, with consistent sets of binaries that can be executed without having to run `dnf install`s in the tasks themselves.


